<div class="modal fade" id="delete_list_{{ list.id }}" tabindex="-1" role="dialog" aria-labelledby="deleteModalList" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title text-dark" id="exampleModalLabel">Suppression de la liste</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>

        <div class="text-dark p-3">
          <h3>Confirmez la suppression de la liste.</h3>
          <h5><i class="fas fa-arrow-right"></i> {{ list.name|capitalize }}</h5>
          <p class="text-danger">
            Attention ! En supprimant cette liste, toutes les tâches associées seront supprimées.<br>
            <b>Cette opération est irréversible.</b>
          </p>
        </div>

        <div class="modal-footer">
          <a href="{{ path('task_list_delete', {id: list.id}) }}" type="button" class="btn btn-danger">Confirmer</a>
          <button type="button" class="btn btn-primary" data-dismiss="modal">Annuler</button>
        </div>
      </div>
    </div>
  </div>